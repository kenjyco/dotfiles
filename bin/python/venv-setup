#!/usr/bin/env bash

env_name=$1
[[ -z "$env_name" ]] && env_name="venv"
[[ ! -d $env_name ]] && python3 -m venv $env_name && $env_name/bin/pip3 install wheel pip --upgrade
[[ -f requirements.txt ]] && $env_name/bin/pip3 install -r requirements.txt --upgrade
