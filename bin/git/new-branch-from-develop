#!/usr/bin/env bash
#
# Create a new local branch from the remote develop

newbranch="$1"
[[ -z "$newbranch" ]] && echo "branch name is required" && exit 1
git stash || exit 1
git checkout -b "$newbranch" origin/develop --no-track
