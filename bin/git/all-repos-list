#!/usr/bin/env bash

level=$1
[[ ! "$level" =~ [0-9]+ ]] && level=4
find ~ -maxdepth $level -type d -name ".git" -print0 | xargs -0 -I {} dirname {} 2>/dev/null | sort \
    | egrep -v '(old-repos|.nvm|.pyenv|.cache|.plugin_install_dir|.beu|projects)'
