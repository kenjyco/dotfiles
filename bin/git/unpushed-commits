#!/usr/bin/env bash

if [[ -z "$1" ]]; then
    git log --oneline @{u}.. 2>/dev/null
else
    path=$(repo-path $1)
    [[ -z "$path" ]] && exit 1
    cd $path
    git log --oneline @{u}.. 2>/dev/null
fi
