#!/usr/bin/env bash

path=$1
[[ -z "$path" ]] && path=$(pwd)
path=$(repo-path $path)
[[ -z "$path" ]] && exit 1
cd $path

the_tag=$(lasttag)
if [[ -n "$the_tag" ]]; then
    git tag -n99 ${the_tag}
else
    echo -e "\nThere was no last tag for the repo"
fi
