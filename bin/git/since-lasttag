#!/usr/bin/env bash

path=$1
[[ -z "$path" ]] && path=$(pwd)
path=$(repo-path $path)
[[ -z "$path" ]] && exit 1
cd $path

tag=$(lasttag $path)

if [[ "$tag" =~ v. ]]; then
    git log --find-renames --no-merges --oneline ${tag}..
fi
